apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: eshop
  namespace: argocd
spec:
  project: default
  destination:
    namespace: eshop
    server: {{ .Values.spec.destination.server }}
  source:
    repoURL: {{ .Values.spec.source.repoURL }}
    targetRevision: {{ .Values.spec.source.targetRevision }}
    path: od-artefacts/63d8bd341f73c24f55c87637/appBlueprints/eshop/v0
  syncPolicy:
    automated:
      prune: true
    syncOptions:
    - CreateNamespace=true